# 운영 환경에서는 반드시 충분히 긴 임의 문자열로 변경하세요.
ADMIN_PASSWORD=change-this-to-a-long-random-password
PORT=3002
DATA_DIR=./data
NODE_ENV=production
FORCE_HTTPS=true
# 여러 출처는 쉼표로 구분합니다.
CORS_ORIGIN=https://jobs.example.com

# 프런트와 API가 다른 도메인일 때만 설정합니다.
# VITE_API_URL=https://api.example.com/api
